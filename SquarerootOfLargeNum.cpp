#include <bits/stdc++.h>
using namespace std;
int main()
{
 int tc;
 cin >> tc;
 while (tc--)
 {
  int n, d;
  cin >> n >> d;
  string s;
  // double ans = sqrt(n);

  // cout << fixed << setprecision(d) << ans << "\n";
 }
 return 0;
}